# =====================================================================
# IKT469 -- RAG semester project
# This repository contains ONLY the runnable RAG project:
#   src/  scripts/  config.yaml  requirements.txt  eval/  README.md
# Everything else (reports, course assignments, data, build artifacts,
# virtual environments) is excluded.
# =====================================================================

# --- Course assignments (separate deliverables, not part of this project) ---
assignments/

# --- Report files and presentation material (not needed to run the project) ---
report/
report.zip
docs/
presentation.py

# --- Data, indexes, models, checkpoints (large, regenerated by the scripts) ---
data/
indexes/
models/
checkpoints/

# --- Python ---
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/
.env

# --- Notebooks ---
.ipynb_checkpoints/

# --- OS / editor ---
.DS_Store
.vscode/
.idea/
